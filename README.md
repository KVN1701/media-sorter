media-sorter is a CLI tool written in Rust which renames and sorts your images and bideos by crestion date. If needed the tool also uses xxh3 hashing to prevent duplicates.

## Installation

```sh
curl -fsSL https://raw.githubusercontent.com/KVN1701/media-sorter/main/install.sh | sh
```

Or manually install and compile from [Releases](https://github.com/KVN1701/media-sorter/releases).

Check your installation by running
```sh
media-sorter --version
Version
```

## Usage

